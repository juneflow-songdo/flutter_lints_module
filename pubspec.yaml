name: flutter_lints_module
description: "It will add the cupertino_icons package to the project and add the necessary settings to the project."
#publish_to: 'none'
repository: https://github.com/juneflow-songdo/flutter_lints_module

version: 1.0.3

environment:
  sdk: '>=3.3.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  go_router:
  june_flow_util:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: #@add

flutter:
  uses-material-design: true